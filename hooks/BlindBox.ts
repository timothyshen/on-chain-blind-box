import { blindBoxABI, readClient } from "@/lib/contract";

export const useBlindBox = () => {
  // purchaseBoxes(uint256 amount) payable - Buy blind boxes
  // openBox(uint256 amount) - Open boxes to reveal NFTs
};
